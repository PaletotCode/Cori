# ─── Cori API — Variáveis de Ambiente ────────────────────────────────────────
# Copie este arquivo para .env e preencha os valores

# ─── App ─────────────────────────────────────────────────────────────────────
APP_NAME="Cori API"
DEBUG=true

# ─── Banco de Dados ───────────────────────────────────────────────────────────
# SQLite local (padrão para dev)
DATABASE_URL=sqlite:///./cori.db

# PostgreSQL (produção) — descomente e preencha:
# DATABASE_URL=postgresql://usuario:senha@localhost:5432/cori_db

# ─── JWT ─────────────────────────────────────────────────────────────────────
# Gere com: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=TROQUE_ISSO_EM_PRODUCAO
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080  # 7 dias

# ─── Google OAuth ─────────────────────────────────────────────────────────────
# Como obter:
#   1. console.cloud.google.com → APIs & Serviços → Credenciais
#   2. Criar credencial OAuth 2.0 → Tipo: Aplicativo da Web
#   3. Copiar "Client ID" (termina em .apps.googleusercontent.com)
#   4. Adicionar origins autorizados: http://localhost:8081 (Expo) e seu domínio
#
# Deixe VAZIO para modo dev (mock de usuário demo):
GOOGLE_CLIENT_ID=

# ─── CORS ─────────────────────────────────────────────────────────────────────
# Em produção, substitua "*" pelo domínio real do app:
# CORS_ORIGINS=["https://cori.app","https://app.cori.com.br"]
CORS_ORIGINS=["*"]
