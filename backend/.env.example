# ─── Cori API — Variáveis de Ambiente ────────────────────────────────────────
# Copie este arquivo para .env e preencha os valores reais.
# NUNCA faça commit do arquivo .env — ele está no .gitignore.

# ─── App ─────────────────────────────────────────────────────────────────────
APP_NAME="Cori API"

# true = logs SQL no terminal (apenas para dev local)
# false = silencioso (Railway / produção)
DEBUG=true

# ─── Banco de Dados ───────────────────────────────────────────────────────────
# DEV LOCAL — SQLite (não precisa instalar nada)
DATABASE_URL=sqlite:///./cori.db

# PRODUÇÃO — PostgreSQL no Railway:
# Railway injeta DATABASE_URL automaticamente quando você vincula o serviço Postgres.
# Não precisa copiar manualmente — fica disponível como variável de ambiente.
# Format: postgresql://USER:PASSWORD@HOST:PORT/DBNAME
# DATABASE_URL=postgresql://postgres:senha@containers-us-west-1.railway.app:7777/railway

# Pool de conexões Postgres (valores razoáveis para MVP)
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30

# ─── JWT ─────────────────────────────────────────────────────────────────────
# Gere uma chave segura com:
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=TROQUE_ISSO_EM_PRODUCAO
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080  # 7 dias

# ─── Google OAuth ─────────────────────────────────────────────────────────────
# Como obter:
#   1. console.cloud.google.com → APIs & Serviços → Credenciais
#   2. Criar credencial OAuth 2.0 → Tipo: Aplicativo da Web
#   3. Copiar "Client ID" (termina em .apps.googleusercontent.com)
#   4. Em "Origins autorizados": adicionar seu domínio Railway + http://localhost:8081
#
# Deixe VAZIO para modo dev (mock de usuário demo):
GOOGLE_CLIENT_ID=

# ─── CORS ─────────────────────────────────────────────────────────────────────
# Dev: aceita qualquer origem
CORS_ORIGINS=["*"]

# Produção: liste apenas origens reais (domínio Railway + Expo)
# CORS_ORIGINS=["https://cori-api-xxxxxxx.railway.app","exp://"]
