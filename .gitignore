# ═══════════════════════════════════════════════════════════
#  Cori — .gitignore  (Commercial Grade)
#  Última actualização: 2026-02-26
# ═══════════════════════════════════════════════════════════

# ── Secrets & Environment ────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
*.crt
secrets/
credentials.json
google-services.json
GoogleService-Info.plist

# ── Python / Backend ─────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/
.Python
pip-wheel-metadata/
MANIFEST

# Virtual environments
backend/.venv/
.venv/
venv/
env/
ENV/

# Database files (local dev only — NEVER commit real patient data)
*.db
*.sqlite
*.sqlite3

# Pytest / coverage
.pytest_cache/
.coverage
coverage.xml
htmlcov/
.tox/

# MyPy / type checkers
.mypy_cache/
.dmypy.json
dmypy.json

# ── Node / Expo / React Native ───────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native build artifacts
android/
ios/
.kotlin/
*.jks
*.keystore
*.mobileprovision
*.p8
*.p12
*.orig.*

# Metro bundler
.metro-health-check*

# TypeScript build info
*.tsbuildinfo

# ── Editor / IDE ─────────────────────────────────────────────
.vscode/settings.json
.vscode/*.code-workspace
.idea/
*.swp
*.swo
*~

# ── OS ───────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ── Logs ─────────────────────────────────────────────────────
logs/
*.log
npm-debug.*
yarn-debug.*
yarn-error.*
pnpm-debug.*

# ── Temporary / Cache ─────────────────────────────────────────
tmp/
temp/
.cache/
*.bak
*.tmp
*.orig

# ── Alembic Note ─────────────────────────────────────────────
# Migration versions are intentionally tracked in version control.
# Only the compiled .pyc files inside migrations/ are excluded
# via the __pycache__ rule above.
